package com.bt.device;

public enum BTDeviceOperationStatus {
    OK,
    ALREADY_RUNNING,
    BT_NOT_FOUND,
    BT_DISABLED,
    INVALID_MAC,
    ERROR_CREATING_SOCKET,
    INVALID_STATE,
}
